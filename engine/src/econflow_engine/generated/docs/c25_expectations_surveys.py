# SPDX-License-Identifier: AGPL-3.0-only
# ============================================================
# GENERATED FILE -- DO NOT EDIT.
# Source: artifacts/node-specs.json (committed) via scripts/gen_schemas.py.
# Rebuild with: python scripts/gen_schemas.py
# ============================================================

"""Tier 3 for category 25-expectations-surveys: descriptions and input examples.

A worker executing a graph must NOT import from here -- this tier is
roughly 80% of the artifact and none of it is needed to run a node.
"""

from typing import Any

NODE_DOCS: dict[str, dict[str, Any]] = {
    'ca_associations': {
        'fn': 'ca_associations',
        'description': 'ca_associations -- category 25-expectations-surveys, method card #257.',
        'args': {'x': "Handle to a NUMERICALLY CODED VECTOR of OBSERVATIONS for the ROW variable (one INTEGER category code PER RESPONDENT; e.g. down=1, same=2, up=3). NOT the ready-made contingency table — the table is built by the node. NOT a CONTINUOUS variable (discretize first: 00-data-utilities/binning). NOT a factor (hard stop): give int(x) — codes 1..k in the order of the levels — and levels(x) as 'x_labels'. CROSS-SECTION ONLY: series handles ARE REJECTED and a Ljung-Box whiteness precheck runs.", 'y': "Handle to the CORRESPONDING coded vector for the COLUMN variable. SAME length as 'x' — PAIRED answers from the SAME respondents.", 'x_name': "LABEL of the ROW variable (NOT data; default 'x'). Used in the axis labels and in the cross-section gate's diagnostics. Must differ from 'y_name'.", 'y_name': "LABEL of the COLUMN variable (NOT data; default 'y').", 'x_levels': 'OPTIONAL COMPLETE set of INTEGER codes of the row variable (e.g. [1,2,3,4] when one questionnaire option was NOT selected by anyone). Must be a SUPERSET of the observed codes — otherwise the categorical encoder would SILENTLY turn the observations into NA. Useful for IDENTICAL coding between two waves of the same survey (comparable cell-by-cell tables). Omitting it, the levels result from the OBSERVED codes.', 'y_levels': "Same as 'x_levels', for the COLUMN variable.", 'x_labels': "OPTIONAL category LABELS (NOT data), ONE-TO-ONE with the SORTED codes (e.g. ['down','same','up']). Must be unique and non-empty (duplicates WOULD MERGE categories). Default: '<x_name>=<code>'.", 'y_labels': "Same as 'x_labels', for the COLUMN variable.", 'na_action': "EXPLICIT NA policy (default 'fail' = hard stop). the cross-tabulation routine SILENTLY drops NA (NA dropped), so the reported n would not be the input's n. With 'omit' the removal happens EXPLICITLY PER PAIR and the count is returned (n_na).", 'max_levels': 'Maximum number of categories PER variable (default 20; permitted [2, 50]). Many levels => a sparse table (the chi-square approximation collapses) and the EXACT Fisher blows up ("FEXACT error"). It is ALSO a safety net against accidentally passing a CONTINUOUS variable.', 'lb_lag': 'Lag of the Ljung-Box whiteness precheck (cross-section gate). If omitted: the documented rule min(10, n/5) (Hyndman & Athanasopoulos, FPP 3rd ed. §5.4). MUST be a positive integer < n.', 'gate_alpha': "Level OF THE CROSS-SECTION GATE in the OPEN interval (0,1) — default 0.05. Rejection threshold ONLY of the Ljung-Box whiteness precheck. ⚠️ It IS a TEST OF SIZE gate_alpha: it blocks, by construction, ~gate_alpha of the proportion of VALID i.i.d. input (live: n=200 -> 1.6%/5.7%/10.1% at 0.01/0.05/0.10). Hence it is DECOUPLED from the test's 'alpha'.", 'ordered': "EXPLICIT declaration: do the ROWS (observations) carry ORDER meaning? True (default, conservative) => the Ljung-Box precheck RUNS. False => genuine cross-section (EXCHANGEABLE rows — e.g. survey respondents) and the precheck is EXPLICITLY skipped (decision = 'pass-unordered'). ⚠️ Ljung-Box depends on ORDER — chi-square/Fisher do NOT. The rejection of series handles/series ALWAYS applies.", 'scale': "MEASUREMENT SCALE of the categories — NEVER guessed (default 'nominal' = safe). With 'ordinal', the ORDINAL measures are added (Goodman-Kruskal Gamma, Kendall tau-b, Stuart tau-c, Somers' D, Pearson/Spearman correlation), which presuppose that the CODES ENCODE ORDER; on a nominal scale they are uninterpretable (the sign changes with the arbitrary order of the categories). CAUTION: Goodman-Kruskal TAU is a NOMINAL PRE measure and is ALWAYS returned.", 'conf_level': 'Confidence level of the CI of ALL the measures (default 0.95).', 'cramer_method': "Confidence interval method of the Cramér's V (DescTools· default 'ncchisq' = non-central chi-square). 'fisher' is the Fisher z-transformation — NO simulation, all options are DETERMINISTIC. Does NOT change the estimate.", 'correct': 'Yates continuity correction on the chi-square-based measures (V / C / T; default False). Allowed ONLY in 2x2 — elsewhere the usual implementations ignore it SILENTLY, so this node errors explicitly instead.'},
        'input_example': {'x': '<raw_handle>', 'y': '<raw_handle>', 'x_name': 'x', 'y_name': 'y', 'na_action': 'fail', 'max_levels': 20, 'gate_alpha': 0.05, 'ordered': True, 'scale': 'nominal', 'conf_level': 0.95, 'cramer_method': 'ncchisq', 'correct': False},
    },
    'ca_chisq': {
        'fn': 'ca_chisq',
        'description': 'ca_chisq -- category 25-expectations-surveys, method card #257.',
        'args': {'x': "Handle to a NUMERICALLY CODED VECTOR of OBSERVATIONS for the ROW variable (one INTEGER category code PER RESPONDENT; e.g. down=1, same=2, up=3). NOT the ready-made contingency table — the table is built by the node. NOT a CONTINUOUS variable (discretize first: 00-data-utilities/binning). NOT a factor (hard stop): give int(x) — codes 1..k in the order of the levels — and levels(x) as 'x_labels'. CROSS-SECTION ONLY: series handles ARE REJECTED and a Ljung-Box whiteness precheck runs.", 'y': "Handle to the CORRESPONDING coded vector for the COLUMN variable. SAME length as 'x' — PAIRED answers from the SAME respondents.", 'x_name': "LABEL of the ROW variable (NOT data; default 'x'). Used in the axis labels and in the cross-section gate's diagnostics. Must differ from 'y_name'.", 'y_name': "LABEL of the COLUMN variable (NOT data; default 'y').", 'x_levels': 'OPTIONAL COMPLETE set of INTEGER codes of the row variable (e.g. [1,2,3,4] when one questionnaire option was NOT selected by anyone). Must be a SUPERSET of the observed codes — otherwise the categorical encoder would SILENTLY turn the observations into NA. Useful for IDENTICAL coding between two waves of the same survey (comparable cell-by-cell tables). Omitting it, the levels result from the OBSERVED codes.', 'y_levels': "Same as 'x_levels', for the COLUMN variable.", 'x_labels': "OPTIONAL category LABELS (NOT data), ONE-TO-ONE with the SORTED codes (e.g. ['down','same','up']). Must be unique and non-empty (duplicates WOULD MERGE categories). Default: '<x_name>=<code>'.", 'y_labels': "Same as 'x_labels', for the COLUMN variable.", 'na_action': "EXPLICIT NA policy (default 'fail' = hard stop). the cross-tabulation routine SILENTLY drops NA (NA dropped), so the reported n would not be the input's n. With 'omit' the removal happens EXPLICITLY PER PAIR and the count is returned (n_na).", 'max_levels': 'Maximum number of categories PER variable (default 20; permitted [2, 50]). Many levels => a sparse table (the chi-square approximation collapses) and the EXACT Fisher blows up ("FEXACT error"). It is ALSO a safety net against accidentally passing a CONTINUOUS variable.', 'lb_lag': 'Lag of the Ljung-Box whiteness precheck (cross-section gate). If omitted: the documented rule min(10, n/5) (Hyndman & Athanasopoulos, FPP 3rd ed. §5.4). MUST be a positive integer < n.', 'gate_alpha': "Level OF THE CROSS-SECTION GATE in the OPEN interval (0,1) — default 0.05. Rejection threshold ONLY of the Ljung-Box whiteness precheck. ⚠️ It IS a TEST OF SIZE gate_alpha: it blocks, by construction, ~gate_alpha of the proportion of VALID i.i.d. input (live: n=200 -> 1.6%/5.7%/10.1% at 0.01/0.05/0.10). Hence it is DECOUPLED from the test's 'alpha'.", 'ordered': "EXPLICIT declaration: do the ROWS (observations) carry ORDER meaning? True (default, conservative) => the Ljung-Box precheck RUNS. False => genuine cross-section (EXCHANGEABLE rows — e.g. survey respondents) and the precheck is EXPLICITLY skipped (decision = 'pass-unordered'). ⚠️ Ljung-Box depends on ORDER — chi-square/Fisher do NOT. The rejection of series handles/series ALWAYS applies.", 'alpha': "Significance level OF THE TEST in the OPEN interval (0,1) — default 0.05. Determines ONLY the 'decision' field; it does NOT change the p-value and does NOT affect the cross-section gate (which has its OWN 'gate_alpha').", 'rule': "Rule for ACCEPTABLE expected frequencies (HARD gate — the usual chi-square routines only warn that the approximation may be incorrect). 'cochran' (default): no expected value < 1 AND <= 20% of cells < 5 (Cochran 1954). 'strict': NO expected value < 5. Failure => use ca_fisher or simulate_p_value=True.", 'correct': "Yates continuity correction (default True). Applies ONLY to 2x2 — elsewhere it is ignored SILENTLY by the usual routines; the output field 'yates_applied' says what ACTUALLY happened. AFFECTS ONLY 'statistic'/'p_value' (the TEST's statistic). THE SIZE MEASURES ARE NOT: 'cramers_v' and 'contribution_share' are ALWAYS computed from the UNCORRECTED Pearson X² ('statistic_pearson'; Cramér 1946 §21.9), so they COINCIDE with ca_associations.", 'simulate_p_value': 'Monte-Carlo p-value instead of the asymptotic/exact path (default False). It is the WAY OUT when the Cochran rule rejects the chi-square (it does not rely on the asymptotic) or when the exact Fisher does not fit in memory ("FEXACT error"). Requires a seed for determinism.', 'B': 'Monte-Carlo replications when simulate_p_value=True (default 2000; minimum 100). Ignored otherwise.', 'seed': "Seed of the Monte-Carlo path (default 1234). The call runs with the seed set AND restores the caller's RNG state => a REPRODUCIBLE p-value without contaminating the RNG stream. Ignored on the algebraic (deterministic) paths."},
        'input_example': {'x': '<raw_handle>', 'y': '<raw_handle>', 'x_name': 'x', 'y_name': 'y', 'na_action': 'fail', 'max_levels': 20, 'gate_alpha': 0.05, 'ordered': True, 'alpha': 0.05, 'rule': 'cochran', 'correct': True, 'simulate_p_value': False, 'B': 2000, 'seed': 1234},
    },
    'ca_contingency': {
        'fn': 'ca_contingency',
        'description': 'ca_contingency -- category 25-expectations-surveys, method card #257.',
        'args': {'x': "Handle to a NUMERICALLY CODED VECTOR of OBSERVATIONS for the ROW variable (one INTEGER category code PER RESPONDENT; e.g. down=1, same=2, up=3). NOT the ready-made contingency table — the table is built by the node. NOT a CONTINUOUS variable (discretize first: 00-data-utilities/binning). NOT a factor (hard stop): give int(x) — codes 1..k in the order of the levels — and levels(x) as 'x_labels'. CROSS-SECTION ONLY: series handles ARE REJECTED and a Ljung-Box whiteness precheck runs.", 'y': "Handle to the CORRESPONDING coded vector for the COLUMN variable. SAME length as 'x' — PAIRED answers from the SAME respondents.", 'x_name': "LABEL of the ROW variable (NOT data; default 'x'). Used in the axis labels and in the cross-section gate's diagnostics. Must differ from 'y_name'.", 'y_name': "LABEL of the COLUMN variable (NOT data; default 'y').", 'x_levels': 'OPTIONAL COMPLETE set of INTEGER codes of the row variable (e.g. [1,2,3,4] when one questionnaire option was NOT selected by anyone). Must be a SUPERSET of the observed codes — otherwise the categorical encoder would SILENTLY turn the observations into NA. Useful for IDENTICAL coding between two waves of the same survey (comparable cell-by-cell tables). Omitting it, the levels result from the OBSERVED codes.', 'y_levels': "Same as 'x_levels', for the COLUMN variable.", 'x_labels': "OPTIONAL category LABELS (NOT data), ONE-TO-ONE with the SORTED codes (e.g. ['down','same','up']). Must be unique and non-empty (duplicates WOULD MERGE categories). Default: '<x_name>=<code>'.", 'y_labels': "Same as 'x_labels', for the COLUMN variable.", 'na_action': "EXPLICIT NA policy (default 'fail' = hard stop). the cross-tabulation routine SILENTLY drops NA (NA dropped), so the reported n would not be the input's n. With 'omit' the removal happens EXPLICITLY PER PAIR and the count is returned (n_na).", 'max_levels': 'Maximum number of categories PER variable (default 20; permitted [2, 50]). Many levels => a sparse table (the chi-square approximation collapses) and the EXACT Fisher blows up ("FEXACT error"). It is ALSO a safety net against accidentally passing a CONTINUOUS variable.', 'lb_lag': 'Lag of the Ljung-Box whiteness precheck (cross-section gate). If omitted: the documented rule min(10, n/5) (Hyndman & Athanasopoulos, FPP 3rd ed. §5.4). MUST be a positive integer < n.', 'gate_alpha': "Level OF THE CROSS-SECTION GATE in the OPEN interval (0,1) — default 0.05. Rejection threshold ONLY of the Ljung-Box whiteness precheck. ⚠️ It IS a TEST OF SIZE gate_alpha: it blocks, by construction, ~gate_alpha of the proportion of VALID i.i.d. input (live: n=200 -> 1.6%/5.7%/10.1% at 0.01/0.05/0.10). Hence it is DECOUPLED from the test's 'alpha'.", 'ordered': "EXPLICIT declaration: do the ROWS (observations) carry ORDER meaning? True (default, conservative) => the Ljung-Box precheck RUNS. False => genuine cross-section (EXCHANGEABLE rows — e.g. survey respondents) and the precheck is EXPLICITLY skipped (decision = 'pass-unordered'). ⚠️ Ljung-Box depends on ORDER — chi-square/Fisher do NOT. The rejection of series handles/series ALWAYS applies."},
        'input_example': {'x': '<raw_handle>', 'y': '<raw_handle>', 'x_name': 'x', 'y_name': 'y', 'na_action': 'fail', 'max_levels': 20, 'gate_alpha': 0.05, 'ordered': True},
    },
    'ca_fisher': {
        'fn': 'ca_fisher',
        'description': 'ca_fisher -- category 25-expectations-surveys, method card #257.',
        'args': {'x': "Handle to a NUMERICALLY CODED VECTOR of OBSERVATIONS for the ROW variable (one INTEGER category code PER RESPONDENT; e.g. down=1, same=2, up=3). NOT the ready-made contingency table — the table is built by the node. NOT a CONTINUOUS variable (discretize first: 00-data-utilities/binning). NOT a factor (hard stop): give int(x) — codes 1..k in the order of the levels — and levels(x) as 'x_labels'. CROSS-SECTION ONLY: series handles ARE REJECTED and a Ljung-Box whiteness precheck runs.", 'y': "Handle to the CORRESPONDING coded vector for the COLUMN variable. SAME length as 'x' — PAIRED answers from the SAME respondents.", 'x_name': "LABEL of the ROW variable (NOT data; default 'x'). Used in the axis labels and in the cross-section gate's diagnostics. Must differ from 'y_name'.", 'y_name': "LABEL of the COLUMN variable (NOT data; default 'y').", 'x_levels': 'OPTIONAL COMPLETE set of INTEGER codes of the row variable (e.g. [1,2,3,4] when one questionnaire option was NOT selected by anyone). Must be a SUPERSET of the observed codes — otherwise the categorical encoder would SILENTLY turn the observations into NA. Useful for IDENTICAL coding between two waves of the same survey (comparable cell-by-cell tables). Omitting it, the levels result from the OBSERVED codes.', 'y_levels': "Same as 'x_levels', for the COLUMN variable.", 'x_labels': "OPTIONAL category LABELS (NOT data), ONE-TO-ONE with the SORTED codes (e.g. ['down','same','up']). Must be unique and non-empty (duplicates WOULD MERGE categories). Default: '<x_name>=<code>'.", 'y_labels': "Same as 'x_labels', for the COLUMN variable.", 'na_action': "EXPLICIT NA policy (default 'fail' = hard stop). the cross-tabulation routine SILENTLY drops NA (NA dropped), so the reported n would not be the input's n. With 'omit' the removal happens EXPLICITLY PER PAIR and the count is returned (n_na).", 'max_levels': 'Maximum number of categories PER variable (default 20; permitted [2, 50]). Many levels => a sparse table (the chi-square approximation collapses) and the EXACT Fisher blows up ("FEXACT error"). It is ALSO a safety net against accidentally passing a CONTINUOUS variable.', 'lb_lag': 'Lag of the Ljung-Box whiteness precheck (cross-section gate). If omitted: the documented rule min(10, n/5) (Hyndman & Athanasopoulos, FPP 3rd ed. §5.4). MUST be a positive integer < n.', 'gate_alpha': "Level OF THE CROSS-SECTION GATE in the OPEN interval (0,1) — default 0.05. Rejection threshold ONLY of the Ljung-Box whiteness precheck. ⚠️ It IS a TEST OF SIZE gate_alpha: it blocks, by construction, ~gate_alpha of the proportion of VALID i.i.d. input (live: n=200 -> 1.6%/5.7%/10.1% at 0.01/0.05/0.10). Hence it is DECOUPLED from the test's 'alpha'.", 'ordered': "EXPLICIT declaration: do the ROWS (observations) carry ORDER meaning? True (default, conservative) => the Ljung-Box precheck RUNS. False => genuine cross-section (EXCHANGEABLE rows — e.g. survey respondents) and the precheck is EXPLICITLY skipped (decision = 'pass-unordered'). ⚠️ Ljung-Box depends on ORDER — chi-square/Fisher do NOT. The rejection of series handles/series ALWAYS applies.", 'alpha': "Significance level OF THE TEST in the OPEN interval (0,1) — default 0.05. Determines ONLY the 'decision' field; it does NOT change the p-value and does NOT affect the cross-section gate (which has its OWN 'gate_alpha').", 'alternative': "Alternative hypothesis (default 'two.sided'). ONE-SIDED ONLY in 2x2: the exact test routine SILENTLY ignores alternative in rxc (it returns the two.sided p-value) — the node blocks it explicitly. Incompatible with simulate_p_value=True.", 'conf_level': 'Confidence level of the odds ratio CI (default 0.95; 2x2 ONLY — an odds ratio is not defined for larger tables).', 'simulate_p_value': 'Monte-Carlo p-value instead of the asymptotic/exact path (default False). It is the WAY OUT when the Cochran rule rejects the chi-square (it does not rely on the asymptotic) or when the exact Fisher does not fit in memory ("FEXACT error"). Requires a seed for determinism.', 'B': 'Monte-Carlo replications when simulate_p_value=True (default 2000; minimum 100). Ignored otherwise.', 'workspace': 'Working memory of the exact FEXACT algorithm (documented default 2e5; minimum 1e4). LIVE-VERIFIED: 4x4 with n=300 blows up with "FEXACT error 6. LDKEY=620 is too small" at 2e5; SOME such tables pass with 2e6, DENSER ones blow up THERE too ("FEXACT error 7"); 12x12 blows up even at 2e8 => then ONLY simulate_p_value.', 'seed': "Seed of the Monte-Carlo path (default 1234). The call runs with the seed set AND restores the caller's RNG state => a REPRODUCIBLE p-value without contaminating the RNG stream. Ignored on the algebraic (deterministic) paths."},
        'input_example': {'x': '<raw_handle>', 'y': '<raw_handle>', 'x_name': 'x', 'y_name': 'y', 'na_action': 'fail', 'max_levels': 20, 'gate_alpha': 0.05, 'ordered': True, 'alpha': 0.05, 'alternative': 'two.sided', 'conf_level': 0.95, 'simulate_p_value': False, 'B': 2000, 'workspace': 200000, 'seed': 1234},
    },
    'qn_balance': {
        'fn': 'qn_balance',
        'description': 'qn_balance -- category 25-expectations-surveys, method card #231.',
        'args': {'data': 'Handle to a DataFrame {y (numeric realized series, e.g. inflation), up, same, down (non-negative shares/counts of respondents who expect y to rise/stay the same/fall)}; all 4 columns of the SAME length, >= 3 periods.', 'forecast_horizon': 'Forecast horizon of the survey in periods (positive integer): how many periods ahead the qualitative question looks.', 'first_period': 'Index of the first period of the survey window (positive integer); default 1 (bal/cp/ra), 11 (ce, needs history).', 'last_period': 'Index of the last period of the survey window; default n - forecast_horizon. first_period < last_period is required and last_period + forecast_horizon <= n.', 'growth_limit': 'Upper bound (cap) on the absolute value of the quantified change, for outlier control (default: no bound).', 'suppress_warnings': "Suppression of the package's runtime warnings (default False)."},
        'input_example': {'data': '<df_handle>', 'forecast_horizon': 1, 'suppress_warnings': False},
    },
    'qn_carlson_parkin': {
        'fn': 'qn_carlson_parkin',
        'description': 'qn_carlson_parkin -- category 25-expectations-surveys, method card #231.',
        'args': {'data': 'Handle to a DataFrame {y (numeric realized series, e.g. inflation), up, same, down (non-negative shares/counts of respondents who expect y to rise/stay the same/fall)}; all 4 columns of the SAME length, >= 3 periods.', 'forecast_horizon': 'Forecast horizon of the survey in periods (positive integer): how many periods ahead the qualitative question looks.', 'first_period': 'Index of the first period of the survey window (positive integer); default 1 (bal/cp/ra), 11 (ce, needs history).', 'last_period': 'Index of the last period of the survey window; default n - forecast_horizon. first_period < last_period is required and last_period + forecast_horizon <= n.', 'limen_type': 'Method for computing the indifference limen (default carlson.parkin=time-varying symmetric): weber.fechner=proportional to the level; constant=fixed (const_limen); symm.series=user symmetric (user_symm_limen); asymm.series=user asymmetric (user_upper/lower_limen).', 'const_limen': "limen_type='constant': fixed limen > 0.", 'user_symm_limen': "limen_type='symm.series': symmetric limen per period, of length n.", 'user_upper_limen': "limen_type='asymm.series': upper limen per period, of length n.", 'user_lower_limen': "limen_type='asymm.series': lower limen per period, of length n.", 'correct_zero': 'Correction of zero shares by correct_by so that the inversion is defined (Nardo 2003; default True).', 'correct_by': 'Amount of the zero-shares correction (default 0.01).', 'growth_limit': 'Upper bound (cap) on the absolute value of the quantified change, for outlier control (default: no bound).', 'distrib_type': 'Expectations distribution for probability-inversion (default normal): normal · logistic · t (Student, with distrib_t_df).', 'distrib_mean': 'Mean of the normal distribution (default 0).', 'distrib_sd': 'Standard deviation of the normal distribution (default 1, > 0).', 'distrib_log_location': 'Location of the logistic distribution (default 0).', 'distrib_log_scale': 'Scale of the logistic distribution (default 1, > 0).', 'distrib_t_df': "Degrees of freedom of the Student-t (positive; only for distrib_type='t').", 'suppress_warnings': "Suppression of the package's runtime warnings (default False)."},
        'input_example': {'data': '<df_handle>', 'forecast_horizon': 1, 'correct_zero': True, 'correct_by': 0.01, 'suppress_warnings': False},
    },
    'qn_conditional_expectations': {
        'fn': 'qn_conditional_expectations',
        'description': 'qn_conditional_expectations -- category 25-expectations-surveys, method card #231.',
        'args': {'data': 'Handle to a DataFrame {y (numeric realized series, e.g. inflation), up, same, down (non-negative shares/counts of respondents who expect y to rise/stay the same/fall)}; all 4 columns of the SAME length, >= 3 periods.', 'forecast_horizon': 'Forecast horizon of the survey in periods (positive integer): how many periods ahead the qualitative question looks.', 'first_period': 'Index of the first period of the survey window (positive integer); default 1 (bal/cp/ra), 11 (ce, needs history).', 'last_period': 'Index of the last period of the survey window; default n - forecast_horizon. first_period < last_period is required and last_period + forecast_horizon <= n.', 'exp_horizon_type': 'Experience horizon over the distribution of past realizations of y (default moving=rolling window of mov_horizon_length periods; fix=fixed window [fix_horizon_start, fix_horizon_end]).', 'mov_horizon_length': "exp_horizon_type='moving': length of the rolling experience window (positive integer; default 10).", 'fix_horizon_start': "exp_horizon_type='fix': first period of the fixed window (default 1).", 'fix_horizon_end': "exp_horizon_type='fix': last period of the fixed window (default 10; >= fix_horizon_start).", 'distrib_param': 'Distribution parameter of y for the conditional expectation (default mean; median more robust to outliers).', 'suppress_warnings': "Suppression of the package's runtime warnings (default False)."},
        'input_example': {'data': '<df_handle>', 'forecast_horizon': 1, 'mov_horizon_length': 10, 'fix_horizon_start': 1, 'fix_horizon_end': 10, 'suppress_warnings': False},
    },
    'qn_regression': {
        'fn': 'qn_regression',
        'description': 'qn_regression -- category 25-expectations-surveys, method card #231.',
        'args': {'data': 'Handle to a DataFrame {y (numeric realized series, e.g. inflation), up, same, down (non-negative shares/counts of respondents who expect y to rise/stay the same/fall)}; all 4 columns of the SAME length, >= 3 periods.', 'forecast_horizon': 'Forecast horizon of the survey in periods (positive integer): how many periods ahead the qualitative question looks.', 'first_period': 'Index of the first period of the survey window (positive integer); default 1 (bal/cp/ra), 11 (ce, needs history).', 'last_period': 'Index of the last period of the survey window; default n - forecast_horizon. first_period < last_period is required and last_period + forecast_horizon <= n.', 'distrib_type': 'Expectations distribution for probability-inversion (default normal): normal · logistic · t (Student, with distrib_t_df).', 'distrib_mean': 'Mean of the normal distribution (default 0).', 'distrib_sd': 'Standard deviation of the normal distribution (default 1, > 0).', 'distrib_log_location': 'Location of the logistic distribution (default 0).', 'distrib_log_scale': 'Scale of the logistic distribution (default 1, > 0).', 'distrib_t_df': "Degrees of freedom of the Student-t (positive; only for distrib_type='t').", 'growth_limit': 'Upper bound (cap) on the absolute value of the quantified change, for outlier control (default: no bound).', 'symmetry_error': 'Standard errors of the limen-symmetry test (default white=heteroskedasticity-consistent· small.sample=HC small-sample correction, MacKinnon-White 1985).', 'suppress_warnings': "Suppression of the package's runtime warnings (default False)."},
        'input_example': {'data': '<df_handle>', 'forecast_horizon': 1, 'suppress_warnings': False},
    },
    'sae_eblup_bhf': {
        'fn': 'sae_eblup_bhf',
        'description': 'sae_eblup_bhf -- category 25-expectations-surveys, method card #233.',
        'args': {'data': 'Handle to a unit-level DataFrame: ONE row per UNIT (sample) with response, auxiliary variables, and the domain column (dom).', 'formula': 'response ~ auxiliaries (unit-level nested-error regression, e.g. "y ~ x1 + x2").', 'dom': 'NAME of the data column with the area codes (domain codes).', 'meanxpop': 'Handle to a DataFrame D×(p+1): 1st column = domain codes; the rest = population means of the p auxiliaries per area (p == number of auxiliaries in the formula).', 'popnsize': 'Handle to a DataFrame D×2: domain codes + population sizes (positive).', 'selectdom': "(optional) vector of domain codes to estimate (subset of the sample's codes); default = all.", 'method': 'Estimation method of the variance components (default REML; ML).'},
        'input_example': {'data': '<df_handle>', 'formula': 'y ~ x', 'dom': '...', 'meanxpop': '<df_handle>', 'popnsize': '<df_handle>'},
    },
    'sae_eblup_fh': {
        'fn': 'sae_eblup_fh',
        'description': 'sae_eblup_fh -- category 25-expectations-surveys, method card #233.',
        'args': {'data': 'Handle to an area-level DataFrame: ONE row per area/domain with the DIRECT estimates (response), the auxiliary variables, and the vardir column. >= 2 areas.', 'formula': 'response ~ auxiliaries; the response is the direct estimates theta-hat_i per area (e.g. "yi ~ x1 + a categorical of (region)").', 'vardir': 'NAME of the data column with the sampling VARIANCES psi_i (variances = SD^2, NOT SD); known & >= 0 for each area.', 'method': 'Estimation method of the variance component (default REML; ML; FH=Fay-Herriot).', 'MAXITER': 'Maximum Fisher-scoring iterations (default 100).', 'PRECISION': 'Convergence tolerance (default 1e-4).', 'B': 'Bootstrap replicates for the Marhuenda goodness-of-fit (default 0 = no bootstrap; >0 = stochastic, seeded).', 'seed': 'Seed before the bootstrap when B>0 (determinism; default 1).'},
        'input_example': {'data': '<df_handle>', 'formula': 'y ~ x', 'vardir': '...', 'MAXITER': 100, 'PRECISION': 0.0001, 'B': 0, 'seed': 1},
    },
    'sae_mse_fh': {
        'fn': 'sae_mse_fh',
        'description': 'sae_mse_fh -- category 25-expectations-surveys, method card #233.',
        'args': {'data': 'Handle to an area-level DataFrame: ONE row per area/domain with the DIRECT estimates (response), the auxiliary variables, and the vardir column. >= 2 areas.', 'formula': 'response ~ auxiliaries; the response is the direct estimates theta-hat_i per area (e.g. "yi ~ x1 + a categorical of (region)").', 'vardir': 'NAME of the data column with the sampling VARIANCES psi_i (variances = SD^2, NOT SD); known & >= 0 for each area.', 'method': 'Estimation method of the variance component (default REML; ML; FH=Fay-Herriot).', 'MAXITER': 'Maximum Fisher-scoring iterations (default 100).', 'PRECISION': 'Convergence tolerance (default 1e-4).', 'B': 'Bootstrap replicates for the Marhuenda goodness-of-fit (default 0 = no bootstrap; >0 = stochastic, seeded).', 'seed': 'Seed before the bootstrap when B>0 (determinism; default 1).'},
        'input_example': {'data': '<df_handle>', 'formula': 'y ~ x', 'vardir': '...', 'MAXITER': 100, 'PRECISION': 0.0001, 'B': 0, 'seed': 1},
    },
    'srv_by_domain': {
        'fn': 'srv_by_domain',
        'description': 'srv_by_domain -- category 25-expectations-surveys, method card #232.',
        'args': {'design': "Handle to the survey.design from srv_design (register field 'design').", 'vars': 'NUMERIC variables for the per-domain estimation.', 'by': '>= 1 grouping column of the design (domains/subpopulations); the estimation is repeated per level combination with the correct design SE per domain.', 'statistic': 'Statistic per domain: svymean (default) or svytotal.', 'deff': 'Design effect per domain. Default False.', 'na_rm': 'Removal of NA before the estimation. Default False.'},
        'input_example': {'design': '<raw_handle>', 'vars': ['PROVIDER/DATASET/SERIES'], 'by': ['PROVIDER/DATASET/SERIES'], 'deff': False, 'na_rm': False},
    },
    'srv_design': {
        'fn': 'srv_design',
        'description': 'srv_design -- category 25-expectations-surveys, method card #232.',
        'args': {'df': 'Handle to a survey-microdata DataFrame (one row per respondent; >= 2 rows). The data pass ONLY as a handle; ids/strata/weights/probs/fpc are its column NAMES.', 'ids': 'Cluster/PSU identifiers: "1" (or "0") for NO clusters (default), or the name(s) of cluster columns (multistage sampling, from the broadest to the narrowest stage).', 'strata': 'Stratification column names (one stratified estimator per stratum); None = no strata. Requires nest=True if the cluster ids are reused within strata.', 'weights': "ONE sampling-weights column (design weight = 1/prob; positive, finite). Mutually exclusive with 'probs'. None & without probs => equal-weighted SRS.", 'probs': "ONE column of selection probabilities in (0,1] (weights = 1/probs); mutually exclusive with 'weights'.", 'fpc': 'Finite population correction: column(s) with population size (or sampling fraction) per stage; reduces the SE when the sample is a large fraction of the population. None = with-replacement (without fpc).', 'nest': 'True => the cluster ids are nested within the strata (re-labelling per stratum). Default False.'},
        'input_example': {'df': '<df_handle>', 'ids': ['PROVIDER/DATASET/SERIES'], 'nest': False},
    },
    'srv_mean': {
        'fn': 'srv_mean',
        'description': 'srv_mean -- category 25-expectations-surveys, method card #232.',
        'args': {'design': "Handle to the survey.design from srv_design (register field 'design').", 'vars': 'NUMERIC variables of the design for the mean estimation (one or more column names; factor is not supported here).', 'deff': 'Return the design effect (Var_design / Var_SRS); >1 => the clustering/weighting increases the variance. Default True.', 'na_rm': 'Removal of NA in the variables before the estimation. Default False.'},
        'input_example': {'design': '<raw_handle>', 'vars': ['PROVIDER/DATASET/SERIES'], 'deff': True, 'na_rm': False},
    },
    'srv_quantile': {
        'fn': 'srv_quantile',
        'description': 'srv_quantile -- category 25-expectations-surveys, method card #232.',
        'args': {'design': "Handle to the survey.design from srv_design (register field 'design').", 'vars': 'NUMERIC variables for the weighted quantiles estimation.', 'quantiles': 'Quantile probabilities STRICTLY in (0,1) (default [0.25,0.5,0.75]); 0 or 1 are rejected (infinite quantiles).', 'na_rm': 'Removal of NA before the estimation. Default False.'},
        'input_example': {'design': '<raw_handle>', 'vars': ['PROVIDER/DATASET/SERIES'], 'na_rm': False},
    },
    'srv_total': {
        'fn': 'srv_total',
        'description': 'srv_total -- category 25-expectations-surveys, method card #232.',
        'args': {'design': "Handle to the survey.design from srv_design (register field 'design').", 'vars': 'NUMERIC variables for the population TOTAL estimation (population total; weighted sum).', 'deff': 'Design effect (see srv_mean). Default True.', 'na_rm': 'Removal of NA before the estimation. Default False.'},
        'input_example': {'design': '<raw_handle>', 'vars': ['PROVIDER/DATASET/SERIES'], 'deff': True, 'na_rm': False},
    },
    'es_fit_bin_density': {
        'fn': 'es_fit_bin_density',
        'description': 'es_fit_bin_density -- category 25-expectations-surveys, method card #574.',
        'args': {'bins': 'Bin edges and reported probabilities per respondent.', 'family': 'Distribution family.', 'open_ended_width': 'Assumed width of open-ended end bins.', 'min_bins': 'Minimum bins required for a parametric fit.'},
        'input_example': {'bins': '<df_handle>', 'family': 'generalised_beta', 'open_ended_width': 1.0, 'min_bins': 3},
    },
    'es_disagreement_uncertainty': {
        'fn': 'es_disagreement_uncertainty',
        'description': 'es_disagreement_uncertainty -- category 25-expectations-surveys, method card #575.',
        'args': {'densities': 'Fitted individual densities or their moments.', 'respondent': 'Respondent identifier.', 'period': 'Survey period.', 'balanced': 'Restrict to a balanced respondent panel.'},
        'input_example': {'densities': '<df_handle>', 'respondent': '...', 'period': '...', 'balanced': False},
    },
    'es_expectations_tests': {
        'fn': 'es_expectations_tests',
        'description': 'es_expectations_tests -- category 25-expectations-surveys, method card #576.',
        'args': {'actual': 'Realised values.', 'forecast': 'Survey forecast.', 'revision': 'Forecast revision for the rigidity test.', 'level': 'Analysis level.', 'horizon': 'Forecast horizon, for the HAC lag.', 'alpha': 'Significance level.'},
        'input_example': {'actual': '<series_handle>', 'forecast': '<series_handle>', 'level': 'consensus', 'horizon': 4, 'alpha': 0.05},
    },
    'es_survey_weights': {
        'fn': 'es_survey_weights',
        'description': 'es_survey_weights -- category 25-expectations-surveys, method card #577.',
        'args': {'data': 'Survey microdata.', 'base_weights': 'Column holding design weights.', 'targets': 'Population control totals.', 'method': 'Method.', 'trim': 'Lower and upper bounds on the weight ratio.', 'max_iter': 'Maximum iterations.'},
        'input_example': {'data': '<df_handle>', 'targets': '<df_handle>', 'method': 'raking', 'trim': [0.2, 5.0], 'max_iter': 100},
    },
    'es_agreement': {
        'fn': 'es_agreement',
        'description': 'es_agreement -- category 25-expectations-surveys, method card #578.',
        'args': {'data': 'Ratings: items by raters.', 'measure': 'Measure.', 'nboot': 'Number of bootstrap replications.', 'seed': 'Seed for the random number generator.', 'conf_level': 'Confidence level for intervals.'},
        'input_example': {'data': '<df_handle>', 'measure': 'kendall_w', 'nboot': 1000, 'conf_level': 0.95},
    },
    'es_polychoric': {
        'fn': 'es_polychoric',
        'description': 'es_polychoric -- category 25-expectations-surveys, method card #578.',
        'args': {'data': 'Ordinal item responses.', 'method': 'Estimator.'},
        'input_example': {'data': '<df_handle>', 'method': 'two_step'},
    },
}
