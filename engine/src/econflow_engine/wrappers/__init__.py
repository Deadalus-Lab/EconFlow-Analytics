# SPDX-License-Identifier: AGPL-3.0-only
"""Method wrappers: one sub-package per METHOD-SELECTION category.

Every module is emitted by ``scripts/gen_wrappers.py --init`` as typed stubs and
is then filled in by hand. The generator NEVER overwrites an existing file.
"""
